const $=s=>document.querySelector(s);
let lastFeed=null,lastGenerated='',lastGeneratedAt=null;
const PRIVATE_REPO='mourchoua-commits/Gvault';
const REGISTRY='modules/vigie/control_tower_sources.json';
const ARCHIVE_CONTRACT='apps/control-tower-master/qrsprite-archive/source-archive/contract.v1.json';
const ARCHIVE_LATEST='apps/control-tower-master/qrsprite-archive/source-archive/refs/latest.json';
const ARCHIVE_ROOT='apps/control-tower-master/qrsprite-archive/source-archive';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function coverage(){const c=lastFeed?.sourceCoverage||null,entries=c?.registeredEntries||c?.entries||[],bad=entries.filter(x=>!['LOADED','LOADED_DIRECTORY'].includes(String(x?.status||'')));return {available:!!c,registered:Number(c?.registered??entries.length||0),loaded:Number(c?.loadedRegistered??c?.loaded??0),missing:Number(c?.missingOrDegradedRegistered??c?.missingOrDegraded??bad.length),bad}}
function lineForGap(x){return `- ${x.engine||'unknown'} | branch=${x.branch||'main'} | kind=${x.kind||'unknown'} | path=${x.path||'—'} | status=${x.status||'UNKNOWN'}`}
function buildPrompt(){
 const c=coverage(),gaps=c.bad.slice(0,120).map(lineForGap).join('\n');
 const observed=c.available?`\nÉTAT OBSERVÉ PAR LA TOWER AU MOMENT DE GÉNÉRER CE MESSAGE\n- registre: ${c.registered}\n- chargées: ${c.loaded}\n- manquantes/dégradées: ${c.missing}\n- ces trous sont des PRIORITÉS DE RAFRAÎCHISSEMENT, pas une réduction du scope FULL :\n${gaps||'- aucun trou observé'}\n`:`\nÉTAT OBSERVÉ PAR LA TOWER\n- aucun FULL INPUT déchiffré dans cette session ; le scope reste FULL_REGISTRY selon le registre privé courant.\n`;
 return `GVAULT_CONTROL_TOWER_ARCHIVE_DELIVERY_PROMPT_V3_1\n\nMission : me fournir le fichier Control Tower le PLUS RÉCENT POSSIBLE au moment de la livraison, depuis l'archive privée canonique configurée. Ne me rends pas un résumé à la place du fichier.\n\nEMPLACEMENT CONFIGURÉ\n- dépôt privé : ${PRIVATE_REPO}\n- contrat d'archive : ${ARCHIVE_CONTRACT}\n- ref mutable : ${ARCHIVE_LATEST}\n- registre qui décide CE QUI DOIT ÊTRE DANS L'ARCHIVE : ${REGISTRY}\n- racine d'archive : ${ARCHIVE_ROOT}\n\nRÈGLE ANTI-AUTO-RÉFÉRENCE\n- distingue toujours RAW_HEADS et EFFECTIVE_HEADS.\n- RAW_HEADS = HEAD Git bruts, conservés comme preuve/audit.\n- EFFECTIVE_HEADS = derniers commits qui ne sont PAS des écritures dérivées de maintenance de l'archive Control Tower.\n- reconnais comme dérivé au minimum le trailer "GVAULT-Control-Tower-Derived: source-archive" et, pour compatibilité historique, le préfixe de commit "control-tower: archive registry-driven QRSprite".\n- un déplacement de main causé uniquement par ces commits dérivés ne doit PAS rendre l'archive périmée.\n- les objets/snapshots/exports/refs de ${ARCHIVE_ROOT} ne doivent jamais être réingérés comme données de l'archive qu'ils décrivent. Leur santé est de la télémétrie hors empreinte.\n\nPROCÉDURE OBLIGATOIRE AVANT DE ME DONNER LE TÉLÉCHARGEMENT\n1. Lis d'abord le contrat d'archive privé puis le registre courant. Ne te fie pas à une liste mémorisée dans ce prompt si le registre a évolué.\n2. Lis refs/latest.json s'il existe. Il n'est qu'un pointeur, jamais une preuve de fraîcheur.\n3. Observe les RAW_HEADS actuels de main, vigie-state, intake-state et audit/action-ledger, puis calcule EFFECTIVE_HEAD_BEFORE selon la règle anti-auto-référence ci-dessus. Relève aussi l'identité/version du registre.\n4. Compare EFFECTIVE_HEAD_BEFORE aux effectiveSourceHeads du snapshot. Pour un ancien snapshot qui ne possède que sourceHeads, interprète-les comme l'état de source au build et tolère uniquement les commits dérivés d'archive prouvés après ces SHA.\n5. Si TOUT correspond et que l'export vérifie ses SHA-256/descripteurs, réutilise l'archive immuable : ne reconstruis pas inutilement.\n6. Si un EFFECTIVE_HEAD diffère, si latest est absent, si l'export manque, si son SHA échoue ou si la sélection n'est plus FULL_REGISTRY, RAFRAÎCHIS l'archive depuis les sources privées actuelles.\n7. Le scope de rafraîchissement est le registre COMPLET : NDJSON sans tranche implicite, historique Git ALL quand demandé, mais exclut du flux Git les commits explicitement dérivés de maintenance source-archive ; sources dérivées de build incluses, sourceCoverage explicite, texte conversationnel brut interdit.\n8. Juste après la collecte/build, relis RAW_HEADS et EFFECTIVE_HEADS. Si EFFECTIVE_HEAD_AFTER != EFFECTIVE_HEAD_BEFORE, ne livre pas comme CURRENT ; recommence sur le nouvel état ou retourne SOURCE_MOVED_RETRY_REQUIRED. Un changement RAW uniquement dérivé de l'archive n'est pas un changement de source.\n9. Écris/actualise une projection privée content-addressed selon le contrat : objets sha256 immuables, snapshot ctarc:<sha256>, refs/latest.json, export control-tower-source-drop.json et un reçu requests/<requestId>.json.\n10. Immédiatement avant de joindre le fichier, refais une dernière revalidation des EFFECTIVE_HEADS. Si ça a encore bougé par une source réelle, rafraîchis avant livraison.\n${observed}\nMODE DE RAFRAÎCHISSEMENT\n- Préférence : SECURE_CANONICAL_PIPELINE. Utilise le pipeline GVAULT existant ; build-control-tower-snapshot-v2.mjs construit le FULL INPUT, build-control-tower-qrsprite-input-v1.mjs fabrique le ctqr: canonique et archive automatiquement le source-drop correspondant sous ${ARCHIVE_ROOT}.\n- Si les runners/secrets serveur ne sont pas exécutables mais que tu disposes du connecteur GitHub privé autorisé : utilise ASSISTED_CONNECTOR_REFRESH. Lis réellement les sources du registre au dernier état disponible, construis/archives le source-drop privé et consigne le reçu. Dans ce mode, NE PRÉTENDS PAS avoir produit un ctqr: canonique. Indique qrsprite.status=LOCAL_SAS_AT_IMPORT ; la Tower créera le ctqru: chiffré sous SAS quand j'importerai le fichier.\n- N'utilise jamais PULSE pour fabriquer une source manquante.\n\nFICHIER À ME FOURNIR\n- nom : control-tower-source-drop.json\n- schéma : GVAULT_CONTROL_TOWER_SOURCE_DROP_V1\n- prends l'export indiqué par le snapshot ctarc: revalidé, idéalement exports/<archiveSha256>/control-tower-source-drop.json.\n- chaque files[] conserve au minimum engine, branch, path, kind, encoding, size, sha256, content.\n- aucun PAT, mot de passe, secret ou donnée interdite par le registre.\n- ne publie JAMAIS cet export plaintext dans Gvault-Pages. Il reste privé et m'est remis comme fichier de téléchargement.\n\nAPRÈS LIVRAISON\n- donne-moi le fichier lui-même, puis un compte rendu court : archiveKey ctarc:, generatedAt, registre/version, RAW_HEADS + EFFECTIVE_HEADS revalidés, nombre de fichiers, couverture loaded/registered/gaps, SHA-256 + taille de l'export, statut QRSprite (MATCHED_CANONICAL_CTQR ou LOCAL_SAS_AT_IMPORT), reçu privé créé et commit privé si réellement effectué.\n- statut final exact : CURRENT_ARCHIVE_DELIVERED / CURRENT_ARCHIVE_WITH_EXPLICIT_GAPS / SOURCE_MOVED_RETRY_REQUIRED / BLOCKED.\n\nRÈGLES\n- ne modifie pas GvaultStable.\n- ne crée pas une seconde autorité de données : le registre et les sources privées restent l'autorité ; l'archive est une projection reproductible.\n- latest est mutable, snapshots/objects sont immuables et vérifiés par digest.\n- ctarc:, ctqr:, ctqrb: et ctqru: sont des types distincts et non substituables.\n- les métadonnées de santé de l'archive sont observables, mais ne doivent pas entrer dans leur propre empreinte/archive.\n- si tu n'as pas réellement revalidé la fraîcheur effective juste avant livraison, ne marque pas CURRENT.\n`;
}
async function copyText(text){try{await navigator.clipboard.writeText(text);return true}catch{}try{const t=$('#ctSourcePromptText');t.value=text;t.focus();t.select();return document.execCommand('copy')}catch{return false}}
function ensureUi(){if($('#ctSourcePromptGenerator'))return;const anchor=$('#ctAdaptiveViews')||$('#ctPulse')||document.querySelector('.kpis')||document.body;const n=document.createElement('section');n.id='ctSourcePromptGenerator';n.innerHTML=`<style>#ctSourcePromptGenerator{margin:0 10px 12px;border:1px solid var(--line);border-radius:10px;background:var(--panel);font:9px ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;overflow:hidden}.ctspHead{display:flex;gap:7px;align-items:center;flex-wrap:wrap;padding:9px}.ctspHead b{color:var(--accent);margin-right:auto}.ctspHead button{min-height:44px}.ctspMeta{padding:0 9px 9px;color:var(--muted);line-height:1.45}.ctspState.ok{color:var(--ok)}.ctspState.warn{color:var(--warn)}#ctSourcePromptDialog{width:min(94vw,860px);max-height:88vh;background:var(--panel);color:var(--text);border:1px solid var(--line);border-radius:10px;padding:0}#ctSourcePromptText{width:100%;min-height:54vh;resize:vertical;border:0;border-top:1px solid var(--line);border-bottom:1px solid var(--line);border-radius:0;padding:10px;font-size:9px;line-height:1.45}#ctSourcePromptDialog .ctspDialogHead,#ctSourcePromptDialog .ctspDialogActions{display:flex;gap:7px;align-items:center;padding:10px}#ctSourcePromptDialog .ctspDialogHead b{margin-right:auto;color:var(--accent)}#ctSourcePromptDialog button{min-height:44px}@media(max-width:620px){.ctspHead button{width:100%}}</style><div class="ctspHead"><b>PROMPT · RÉCUPÉRER ARCHIVE CONTROL TOWER</b><span id="ctspState" class="ctspState">prêt</span><button id="ctspGenerate">GÉNÉRER + COPIER</button><button id="ctspView">VOIR</button></div><div id="ctspMeta" class="ctspMeta">Le message pointe vers l'archive privée configurée, revalide sa fraîcheur EFFECTIVE_HEADS puis demande le vrai source-drop prêt à importer.</div><dialog id="ctSourcePromptDialog"><div class="ctspDialogHead"><b>PROMPT ARCHIVE CONTROL TOWER</b><span id="ctspDialogState"></span></div><textarea id="ctSourcePromptText" spellcheck="false"></textarea><div class="ctspDialogActions"><button id="ctspCopy">COPIER</button><form method="dialog"><button>FERMER</button></form></div></dialog>`;anchor.insertAdjacentElement('afterend',n);$('#ctspGenerate').onclick=()=>generate(true);$('#ctspView').onclick=()=>generate(false);$('#ctspCopy').onclick=async()=>{$('#ctspDialogState').textContent=await copyText($('#ctSourcePromptText').value)?'COPIÉ ✓':'COPIE MANUELLE'}}
function render(){ensureUi();const c=coverage(),s=$('#ctspState'),m=$('#ctspMeta');if(c.available){s.textContent=c.missing?`ARCHIVE FULL · ${c.missing} PRIORITÉ(S)`:'ARCHIVE FULL · COUVERTURE OK';s.className='ctspState '+(c.missing?'warn':'ok');m.textContent=`scope FULL_REGISTRY · ${c.loaded}/${c.registered} actuellement observées · le prompt revalidera EFFECTIVE_HEADS avant livraison.`}else{s.textContent='ARCHIVE À REVALIDER';s.className='ctspState warn';m.textContent='Le prompt relira le contrat privé, le registre et les EFFECTIVE_HEADS avant de fournir le source-drop.'}}
async function generate(copy){ensureUi();lastGenerated=buildPrompt();lastGeneratedAt=new Date();$('#ctSourcePromptText').value=lastGenerated;$('#ctspDialogState').textContent=`${lastGeneratedAt.toLocaleTimeString()} · ${lastGenerated.length} caractères`;let copied=false;if(copy)copied=await copyText(lastGenerated);$('#ctspState').textContent=copy?(copied?'PROMPT ARCHIVE COPIÉ ✓':'PROMPT ARCHIVE GÉNÉRÉ'):'PROMPT ARCHIVE GÉNÉRÉ';$('#ctspState').className='ctspState '+(copied?'ok':'');if(!copy||!copied)$('#ctSourcePromptDialog').showModal();window.dispatchEvent(new CustomEvent('gvault:control-tower-source-prompt',{detail:{schema:'GVAULT_CONTROL_TOWER_ARCHIVE_DELIVERY_PROMPT_V3_1',generatedAt:lastGeneratedAt.toISOString(),copied,coverage:coverage(),archiveContract:ARCHIVE_CONTRACT,preferredDeliverable:'control-tower-source-drop.json'}}));return lastGenerated}
window.addEventListener('gvault:control-tower-feed-decrypted',e=>{lastFeed=e.detail?.feed||null;render()});
window.addEventListener('gvault:private-tool-session-expired',()=>{lastFeed=null;render()});
ensureUi();render();
window.GVAULT_CONTROL_TOWER_SOURCE_PROMPT_V1=Object.freeze({schema:'GVAULT_CONTROL_TOWER_ARCHIVE_DELIVERY_PROMPT_V3_1',generate:()=>generate(false),copy:()=>generate(true),getState:()=>({hasFullFeed:!!lastFeed,lastGeneratedAt:lastGeneratedAt?.toISOString()||null,coverage:coverage(),archiveContract:ARCHIVE_CONTRACT,archiveLatest:ARCHIVE_LATEST,preferredDeliverable:'control-tower-source-drop.json'})});
import('./archive-weekly-lever-v1.mjs?v=1').catch(e=>console.warn('CONTROL_TOWER_WEEKLY_LEVER_LOAD_FAILED',e));
