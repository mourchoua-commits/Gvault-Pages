(()=>{'use strict';
const SCHEMA='GTHINK_PUBLIC_RESPONDER_V12_DIRECT_REMOTE';
const BLOB_SCHEMA='GVAULT_UNIVERSAL_BLOB_V1';
const NAME='GThink';
const SCRIPT_BASE=new URL('.',document.currentScript?.src||location.href);
const BRIDGE_URL=new URL('gthink-public-private-bridge.js?v=5',SCRIPT_BASE).href;
const DUAL_ROUTER_URL=new URL('gthink-dual-kernel-router.js?v=1',SCRIPT_BASE).href;
let attached=false,connected=false,heartbeat=null,bridgeLoad=null,routerLoad=null,unregister=null;
function uid(prefix='blob'){return `${prefix}-${crypto.randomUUID?.()||`${Date.now().toString(36)}-${Math.random().toString(36).slice(2)}`}`}
function clean(v){return String(v??'').trim()}
function api(){return window.GVAULT_AGENT_LIVE_BLOB}
function loadBridge(){if(window.GTHINK_PUBLIC_PRIVATE_BRIDGE?.askRequest)return Promise.resolve(window.GTHINK_PUBLIC_PRIVATE_BRIDGE);if(bridgeLoad)return bridgeLoad;bridgeLoad=new Promise(resolve=>{const existing=document.querySelector('script[data-gthink-public-private-bridge="V5"]');if(existing){const started=Date.now(),tick=()=>window.GTHINK_PUBLIC_PRIVATE_BRIDGE?.askRequest?resolve(window.GTHINK_PUBLIC_PRIVATE_BRIDGE):Date.now()-started>3000?resolve(null):setTimeout(tick,25);tick();return}const s=document.createElement('script');s.src=BRIDGE_URL;s.async=false;s.setAttribute('data-gthink-public-private-bridge','V5');s.onload=()=>resolve(window.GTHINK_PUBLIC_PRIVATE_BRIDGE||null);s.onerror=()=>resolve(null);(document.head||document.documentElement).appendChild(s)}).finally(()=>{bridgeLoad=null});return bridgeLoad}
function loadDualRouter(){if(window.GTHINK_DUAL_KERNEL_ROUTER?.respond)return Promise.resolve(window.GTHINK_DUAL_KERNEL_ROUTER);if(routerLoad)return routerLoad;routerLoad=new Promise(resolve=>{const existing=document.querySelector('script[data-gthink-dual-kernel-router="V1"]');if(existing){const started=Date.now(),tick=()=>window.GTHINK_DUAL_KERNEL_ROUTER?.respond?resolve(window.GTHINK_DUAL_KERNEL_ROUTER):Date.now()-started>4000?resolve(null):setTimeout(tick,25);tick();return}const s=document.createElement('script');s.src=DUAL_ROUTER_URL;s.async=false;s.setAttribute('data-gthink-dual-kernel-router','V1');s.onload=()=>resolve(window.GTHINK_DUAL_KERNEL_ROUTER||null);s.onerror=()=>resolve(null);(document.head||document.documentElement).appendChild(s)}).finally(()=>{routerLoad=null});return routerLoad}
async function responder(request){const message=clean(request?.payload?.message||request?.text);if(!message)throw new Error('gthink_empty_message');const bridge=await loadBridge();if(!bridge?.askRequest)throw new Error('gthink_direct_bridge_unavailable');const s=await bridge.status();if(s?.configured!==true)throw new Error(s?.error||'gthink_direct_bridge_not_configured');return bridge.askRequest(request)}
function emit(kind,payload={},text){const a=api();if(!a?.speak)return;a.speak({schema:BLOB_SCHEMA,blobId:uid('gthink-state'),parentBlobId:null,conversationId:'gthink-public-listener',kind,role:'gthink',from:NAME,to:'public.bus',intent:kind==='gthink.listener.ready'?'announce_responder_ready':'announce_responder_waiting',language:'fr',at:new Date().toISOString(),surface:'Gvault-Pages',streamUrl:a.streamUrl,text,payload:{...payload,schema:SCHEMA,streamUrl:a.streamUrl},understoodBy:['GThink','GThinkPelliculeBridge','GThinkMini','public-kernel','gateway-adapter','public-ui','private-bridge'],silent:true,muted:false})}
async function syncConnection(){const a=api();let s={configured:false,error:'gthink_direct_bridge_unavailable'};try{const bridge=await loadBridge();if(bridge?.status)s=await bridge.status()}catch(e){s={configured:false,error:clean(e?.message||e)}}const ready=s?.configured===true,routerActive=!!window.GTHINK_DUAL_KERNEL_ROUTER?.respond;if(ready&&!connected&&!routerActive&&a?.registerResponder){unregister=a.registerResponder(responder,NAME);connected=true}if((!ready||routerActive)&&connected){try{unregister?.()}catch{}unregister=null;connected=false}window.GTHINK_OFFLINE_ONLY=false;if(routerActive){emit(ready?'gthink.primary.ready':'gthink.primary.waiting',{state:ready?'primary_ready':'primary_waiting',name:NAME,mode:'server-private-direct-pellicule',transport:'pellicule-direct',privateBridgeConfigured:ready,error:ready?null:(s?.error||'gthink_private_bridge_not_deployed')},ready?'GThink principal ready':'GThink principal waiting');return ready}if(ready)emit('gthink.listener.ready',{state:'listener_ready',name:NAME,mode:'server-private-direct-pellicule',transport:'pellicule-direct',pelliculeConfigured:true,pelliculeFrameBytes:s?.frameBytes||null,privateBridgeConfigured:true,serverPrivateConfigured:true,offlineCapable:false},'GThink direct ready');else emit('gthink.listener.waiting',{state:'listener_waiting',name:NAME,mode:'server-private-direct-pellicule',transport:'pellicule-direct',pelliculeConfigured:true,privateBridgeConfigured:false,serverPrivateConfigured:false,error:s?.error||'gthink_private_bridge_not_deployed'},'GThink direct waiting');return ready}
function attach(){if(attached)return true;const a=api();if(!a?.speak)return false;attached=true;void syncConnection();heartbeat=setInterval(()=>void syncConnection(),4000);window.GTHINK_PUBLIC_RESPONDER=Object.freeze({schema:SCHEMA,name:NAME,engine:'private-direct-pellicule',attached:true,transport:'pellicule-direct',respond:responder,syncConnection,get connected(){return connected},get bridge(){return window.GTHINK_PUBLIC_PRIVATE_BRIDGE||null}});void loadDualRouter();return true}
if(!attach()){let tries=0;const timer=setInterval(()=>{tries++;if(attach()||tries>240)clearInterval(timer)},25)}
})();
(()=>{'use strict';
if(window.GTHINK_RESPONSE_WORD_FLOW||document.querySelector('script[data-gthink-response-word-flow]'))return;
const current=document.currentScript?.src||location.href;
const script=document.createElement('script');
script.src=new URL('gthink-response-word-flow.js?v=1',current).href;
script.dataset.gthinkResponseWordFlow='V1';
script.async=false;
(document.head||document.documentElement).appendChild(script);
})();
