(()=>{'use strict';
const SCHEMA='GTHINK_PUBLIC_RESPONDER_V11_PELLICULE_E2E';
const BLOB_SCHEMA='GVAULT_UNIVERSAL_BLOB_V1';
const NAME='GThink';
const SCRIPT_BASE=new URL('.',document.currentScript?.src||location.href);
const NATIVE_URL=new URL('gthink-public-native-engine.js?v=1',SCRIPT_BASE).href;
const BRIDGE_URL=new URL('gthink-public-private-bridge.js?v=4',SCRIPT_BASE).href;
let attached=false,connected=false,heartbeat=null,nativeLoad=null,bridgeLoad=null,unregister=null;
function uid(prefix='blob'){return `${prefix}-${crypto.randomUUID?.()||`${Date.now().toString(36)}-${Math.random().toString(36).slice(2)}`}`}
function clean(v){return String(v??'').trim()}
function api(){return window.GVAULT_AGENT_LIVE_BLOB}
function loadScript(src,attr,value,ready,slot){if(ready())return Promise.resolve(ready());if(slot.current)return slot.current;slot.current=new Promise(resolve=>{const existing=document.querySelector(`script[${attr}]`);if(existing){const start=Date.now(),tick=()=>ready()?resolve(ready()):Date.now()-start>3000?resolve(null):setTimeout(tick,25);tick();return}const s=document.createElement('script');s.src=src;s.async=false;s.setAttribute(attr,value);s.onload=()=>resolve(ready()||null);s.onerror=()=>resolve(null);(document.head||document.documentElement).appendChild(s)}).finally(()=>{slot.current=null});return slot.current}
async function ensureNative(){const slot={get current(){return nativeLoad},set current(v){nativeLoad=v}};return loadScript(NATIVE_URL,'data-gthink-public-native-engine','V1',()=>window.GTHINK_PUBLIC_NATIVE_ENGINE?.answer?window.GTHINK_PUBLIC_NATIVE_ENGINE:null,slot)}
async function ensureBridge(){const slot={get current(){return bridgeLoad},set current(v){bridgeLoad=v}};return loadScript(BRIDGE_URL,'data-gthink-public-private-bridge','V4',()=>window.GTHINK_PUBLIC_PRIVATE_BRIDGE?.askRequest?window.GTHINK_PUBLIC_PRIVATE_BRIDGE:null,slot)}
async function responder(request){const message=clean(request?.payload?.message||request?.text);if(!message)throw new Error('gthink_empty_message');const bridge=await ensureBridge();if(!bridge?.askRequest)throw new Error('gthink_pellicule_bridge_unavailable');const s=await bridge.status();if(s?.configured!==true)throw new Error(s?.error||'gthink_pellicule_bridge_not_configured');return bridge.askRequest(request)}
function emit(kind,payload={},text){const a=api();if(!a?.speak)return;a.speak({schema:BLOB_SCHEMA,blobId:uid('gthink-state'),parentBlobId:null,conversationId:'gthink-public-listener',kind,role:'gthink',from:NAME,to:'public.bus',intent:kind==='gthink.listener.ready'?'announce_responder_ready':'announce_responder_waiting',language:'fr',at:new Date().toISOString(),surface:'Gvault-Pages',streamUrl:a.streamUrl,text,payload:{...payload,schema:SCHEMA,streamUrl:a.streamUrl},understoodBy:['GThink','GThinkPelliculeBridge','GThinkMini','public-kernel','gateway-adapter','public-ui','private-bridge'],silent:true,muted:false})}
async function syncConnection(){const a=api();let bridgeStatus={configured:false},nativeStatus={configured:false};try{const bridge=await ensureBridge();if(bridge?.status)bridgeStatus=await bridge.status()}catch(e){bridgeStatus={configured:false,error:clean(e?.message||e)}}try{const native=await ensureNative();if(native?.status)nativeStatus=await native.status()}catch(e){nativeStatus={configured:false,error:clean(e?.message||e)}}const bridgeReady=bridgeStatus?.configured===true,ready=bridgeReady;if(ready&&!connected&&a?.registerResponder){unregister=a.registerResponder(responder,NAME);connected=true}if(!ready&&connected){try{unregister?.()}catch{}unregister=null;connected=false}window.GTHINK_OFFLINE_ONLY=false;if(ready)emit('gthink.listener.ready',{state:'listener_ready',name:NAME,mode:bridgeStatus?.serverConfigured?'server-private-pellicule':'client-private-pellicule',transport:'pellicule-e2e',pelliculeConfigured:true,pelliculeFrameBytes:bridgeStatus?.frameBytes||null,privateBridgeConfigured:true,serverPrivateConfigured:bridgeStatus?.serverConfigured===true,publicNativeDiagnostic:nativeStatus?.configured===true,offlineCapable:bridgeStatus?.offlineCapable===true},'GThink pont-pellicule ready');else emit('gthink.listener.waiting',{state:'listener_waiting',name:NAME,mode:'pellicule-e2e',pelliculeConfigured:false,publicNativeDiagnostic:nativeStatus?.configured===true,error:bridgeStatus?.error||null},'GThink pont-pellicule waiting');return ready}
function attach(){if(attached)return true;const a=api();if(!a?.speak)return false;attached=true;void syncConnection();heartbeat=setInterval(()=>void syncConnection(),4000);window.GTHINK_PUBLIC_RESPONDER=Object.freeze({schema:SCHEMA,name:NAME,engine:'pellicule-e2e',attached:true,transport:'pellicule-e2e',respond:responder,syncConnection,get connected(){return connected},get native(){return window.GTHINK_PUBLIC_NATIVE_ENGINE||null},get bridge(){return window.GTHINK_PUBLIC_PRIVATE_BRIDGE||null}});return true}
if(!attach()){let tries=0;const timer=setInterval(()=>{tries++;if(attach()||tries>240)clearInterval(timer)},25)}
})();