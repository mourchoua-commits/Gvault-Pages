(()=>{'use strict';
const SCHEMA='GTHINK_PUBLIC_PRIVATE_BRIDGE_CLIENT_V2_LOCAL_PRIVATE_FALLBACK';
const BLOB_SCHEMA='GVAULT_UNIVERSAL_BLOB_V1';
const REQUEST_SCHEMA='GTHINK_PUBLIC_PRIVATE_BRIDGE_REQUEST_V1';
const RESPONSE_SCHEMA='GTHINK_PUBLIC_PRIVATE_BRIDGE_RESPONSE_V1';
const SCRIPT_BASE=new URL('.',document.currentScript?.src||location.href);
const CONFIG_URL=new URL('gvault-agent-gateway.json',SCRIPT_BASE).href;
const LOCAL_WORKER_URL=new URL('gthink-client-private-worker.js?v=1',SCRIPT_BASE).href;
const CONFIG_TTL_MS=15000;
const pending=new Map(),workerPending=new Map();
let config=null,configAt=0,worker=null;
function api(){return window.GVAULT_AGENT_LIVE_BLOB}
function uid(prefix='gpubpriv'){return `${prefix}-${crypto.randomUUID?.()||`${Date.now().toString(36)}-${Math.random().toString(36).slice(2)}`}`}
function clean(v){return String(v??'').trim()}
function bytes(v){return new TextEncoder().encode(String(v??'')).byteLength}
async function sha256(text){const d=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(String(text??'')));return [...new Uint8Array(d)].map(x=>x.toString(16).padStart(2,'0')).join('')}
function sessionId(){let id='';try{id=sessionStorage.getItem('gvault.agent.live.session.v1')||''}catch{}if(!id){id=uid('gas');try{sessionStorage.setItem('gvault.agent.live.session.v1',id)}catch{}}return id}
function emit(kind,payload={},parentBlobId=null,meta={}){const a=api();if(!a?.speak)return null;return a.speak({schema:BLOB_SCHEMA,blobId:uid(),parentBlobId,conversationId:meta.conversationId||payload?.conversationId||sessionId(),kind,role:meta.role||'bridge',from:meta.from||'GThinkPublicPrivateBridge',to:meta.to||'public.bus',intent:meta.intent||'route_public_private',language:'fr',at:new Date().toISOString(),surface:'Gvault-Pages',streamUrl:a.streamUrl,text:meta.text,display:meta.text,payload:{...payload,bridgeSchema:SCHEMA},understoodBy:['GThink','GThinkMini','public-kernel','public-ui','private-bridge'],silent:true,muted:false})}
async function loadConfig(force=false){if(!force&&config&&Date.now()-configAt<CONFIG_TTL_MS)return config;try{const r=await fetch(CONFIG_URL+'?ts='+Date.now(),{cache:'no-store',credentials:'omit'});if(!r.ok)throw new Error('gthink_bridge_config_http_'+r.status);config=await r.json();configAt=Date.now();return config}catch(e){if(config)return config;return {schema:'GVAULT_AGENT_GATEWAY_CONFIG_V2',status:'LOCAL_PRIVATE_FALLBACK',baseUrl:null,model:null,configError:clean(e?.message||e)}}}
function bridgeAdapter(c){const base=clean(c?.baseUrl||c?.httpAdapter?.baseUrl).replace(/\/+$/,'');const path=clean(c?.privateBridgePath)||'/v1/gthink-private-bridge';if(!base)return null;return {url:base+(path.startsWith('/')?path:'/'+path),path,schema:c?.privateBridgeSchema||RESPONSE_SCHEMA}}
function historyFor(request){return (Array.isArray(request?.payload?.history)?request.payload.history:[]).slice(-12).map(x=>({role:x?.role==='assistant'?'assistant':'user',content:clean(x?.content)})).filter(x=>x.content)}
function requestMessage(request){return clean(request?.payload?.message||request?.text)}
function ensureWorker(){if(worker)return worker;worker=new Worker(LOCAL_WORKER_URL,{name:'GThinkClientPrivate'});worker.onmessage=e=>{const d=e.data||{},p=workerPending.get(d.id);if(!p)return;workerPending.delete(d.id);clearTimeout(p.timer);if(d.type==='pong')p.resolve(d);else if(d.ok===true)p.resolve(d.result||{});else p.reject(new Error(clean(d.error)||'gthink_client_private_worker_error'))};worker.onerror=e=>{for(const [id,p] of workerPending){clearTimeout(p.timer);p.reject(new Error(clean(e?.message)||'gthink_client_private_worker_crash'));workerPending.delete(id)};try{worker.terminate()}catch{}worker=null};return worker}
function callWorker(type,payload,timeoutMs=5000){return new Promise((resolve,reject)=>{let w;try{w=ensureWorker()}catch(e){reject(e);return}const id=uid('w');const timer=setTimeout(()=>{workerPending.delete(id);reject(new Error('gthink_client_private_worker_timeout'))},timeoutMs);workerPending.set(id,{resolve,reject,timer});w.postMessage({type,id,...payload})})}
async function localStatus(){try{await callWorker('ping',{},1200);return {configured:true,mode:'client-private-worker',path:'worker:gthink-client-private',offlineCapable:true,serverConfigured:false}}catch(e){return {configured:false,mode:'client-private-worker',path:'worker:gthink-client-private',offlineCapable:true,serverConfigured:false,error:clean(e?.message||e)}}}
async function performRemote(request,c,adapter,message,messageSha,correlationId,body){emit('gthink.private.handoff',{state:'handoff',correlationId,publicGatewayBlobId:request.blobId,publicUserBlobId:request.parentBlobId||request?.payload?.requestBlobId||null,messageSha256:messageSha,messageBytes:bytes(message),privateBridgePath:adapter.path,privatePlane:'server-private'},request.blobId,{role:'bridge',from:'GThinkPublicPrivateBridge',to:'private-bridge',intent:'handoff_public_blob_to_private',conversationId:request.conversationId});let r;try{r=await fetch(adapter.url,{method:'POST',headers:{'content-type':'application/json'},credentials:'omit',cache:'no-store',referrerPolicy:'no-referrer',body:JSON.stringify(body)})}catch(e){throw new Error('gthink_private_bridge_network_error')}let data=null;try{data=await r.json()}catch{}if(!r.ok||data?.ok!==true)throw new Error(clean(data?.error)||`gthink_private_bridge_http_${r.status}`);if(data?.schema!==RESPONSE_SCHEMA)throw new Error('gthink_private_bridge_response_schema_invalid');const release=data?.release;if(release?.allowPublic!==true)throw new Error('gthink_private_release_not_authorized');const text=clean(release.text);if(!text)throw new Error('gthink_private_release_empty');if(bytes(text)!==Number(release.byteLength))throw new Error('gthink_private_release_byte_length_mismatch');const releaseSha=await sha256(text);if(releaseSha!==clean(release.sha256).toLowerCase())throw new Error('gthink_private_release_sha256_mismatch');emit('gthink.private.release',{state:'released',correlationId:data.correlationId||correlationId,publicGatewayBlobId:request.blobId,privateRequestBlobId:data?.privateRequestRef?.blobId||null,privateResponseBlobId:data?.privateResponseRef?.blobId||null,publicTextSha256:releaseSha,publicTextBytes:bytes(text),releasePolicy:release.policy||'EXPLICIT_DECLASSIFICATION_V1',allowPublic:true,privatePlane:'server-private'},request.blobId,{role:'bridge',from:'private-bridge',to:'public-kernel',intent:'confirm_explicit_public_release',conversationId:request.conversationId});return {schema:SCHEMA,text,engine:'gthink-private-bridge',model:data?.model||c?.model||null,correlationId:data.correlationId||correlationId,privateRequestRef:data?.privateRequestRef||null,privateResponseRef:data?.privateResponseRef||null,publicTextSha256:releaseSha,releasePolicy:release.policy||null,privatePlane:'server-private',actionsAuthorized:false}}
async function performLocal(request,message,messageSha,correlationId,body){const privateRequestId=uid('private-local-request');emit('gthink.private.handoff',{state:'handoff',correlationId,publicGatewayBlobId:request.blobId,publicUserBlobId:request.parentBlobId||request?.payload?.requestBlobId||null,messageSha256:messageSha,messageBytes:bytes(message),privateBridgePath:'worker:gthink-client-private',privatePlane:'client-private-worker',credentialFree:true},request.blobId,{role:'bridge',from:'GThinkPublicPrivateBridge',to:'client-private-worker',intent:'handoff_public_blob_to_local_private_worker',conversationId:request.conversationId});const result=await callWorker('request',{request:body},6000);const text=clean(result?.text);if(!text)throw new Error('gthink_client_private_release_empty');const releaseSha=await sha256(text),privateResponseId=uid('private-local-response');emit('gthink.private.accepted',{state:'accepted',correlationId,publicGatewayBlobId:request.blobId,privateRequestBlobId:privateRequestId,privateRequestSha256:messageSha,privatePlane:'client-private-worker'},request.blobId,{role:'bridge',from:'client-private-worker',to:'public-kernel',intent:'confirm_local_private_acceptance',conversationId:request.conversationId});emit('gthink.private.release',{state:'released',correlationId,publicGatewayBlobId:request.blobId,privateRequestBlobId:privateRequestId,privateResponseBlobId:privateResponseId,publicTextSha256:releaseSha,publicTextBytes:bytes(text),releasePolicy:'LOCAL_DEVICE_DECLASSIFICATION_V1',allowPublic:true,privatePlane:'client-private-worker',credentialFree:true},request.blobId,{role:'bridge',from:'client-private-worker',to:'public-kernel',intent:'confirm_local_private_public_release',conversationId:request.conversationId});return {schema:SCHEMA,text,engine:result?.engine||'gthink-client-private-worker',model:result?.model||null,correlationId,privateRequestRef:{blobId:privateRequestId,sha256:messageSha,plane:'client-private-worker'},privateResponseRef:{blobId:privateResponseId,sha256:releaseSha,plane:'client-private-worker'},publicTextSha256:releaseSha,releasePolicy:'LOCAL_DEVICE_DECLASSIFICATION_V1',privatePlane:'client-private-worker',credentialFree:true,actionsAuthorized:false}}
async function perform(request){const message=requestMessage(request);if(!message)throw new Error('gthink_bridge_empty_message');const c=await loadConfig(),adapter=bridgeAdapter(c),messageSha=await sha256(message),correlationId=uid('corr');const body={schema:REQUEST_SCHEMA,sessionId:sessionId(),correlationId,benchmark:request?.payload?.benchmark===true,publicBlob:{blobId:request.blobId,conversationId:request.conversationId||sessionId(),text:message,byteLength:bytes(message),sha256:messageSha},history:historyFor(request)};if(adapter){try{return await performRemote(request,c,adapter,message,messageSha,correlationId,body)}catch(e){emit('gthink.private.error',{state:'server_private_unavailable',correlationId,publicGatewayBlobId:request.blobId,error:clean(e?.message||e).slice(0,160),fallback:'client-private-worker'},request.blobId,{role:'bridge',from:'GThinkPublicPrivateBridge',to:'public-kernel',intent:'fallback_to_local_private_worker',conversationId:request.conversationId})}}return performLocal(request,message,messageSha,correlationId,body)}
function askRequest(request){const id=request?.blobId;if(!id)return Promise.reject(new Error('gthink_bridge_request_id_required'));if(pending.has(id))return pending.get(id);const p=perform(request).finally(()=>{setTimeout(()=>pending.delete(id),5000)});pending.set(id,p);return p}
async function status(){const c=await loadConfig(true),a=bridgeAdapter(c);if(a)return {schema:SCHEMA,configured:true,mode:'server-private',serverConfigured:true,offlineCapable:false,url:a.url,path:a.path,responseSchema:a.schema,status:c?.status||null,model:c?.model||null};const local=await localStatus();return {schema:SCHEMA,...local,responseSchema:RESPONSE_SCHEMA,status:c?.status||'LOCAL_PRIVATE_FALLBACK',model:null,configBaseUrl:false}}
window.GTHINK_PUBLIC_PRIVATE_BRIDGE=Object.freeze({schema:SCHEMA,requestSchema:REQUEST_SCHEMA,responseSchema:RESPONSE_SCHEMA,askRequest,status,reload:()=>loadConfig(true),sha256});
emit('gthink.private.bridge.ready',{state:'client_ready',requestSchema:REQUEST_SCHEMA,responseSchema:RESPONSE_SCHEMA,serverOptional:true,localPrivateWorker:true},null,{role:'bridge',from:'GThinkPublicPrivateBridge',to:'public.bus',intent:'announce_private_bridge_client_ready'});
})();
